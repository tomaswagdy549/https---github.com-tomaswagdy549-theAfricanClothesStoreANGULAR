import { GetAllProductsDTO } from './get-all-products-dto';

describe('GetAllProductsDTO', () => {
  it('should create an instance', () => {
    expect(new GetAllProductsDTO()).toBeTruthy();
  });
});
