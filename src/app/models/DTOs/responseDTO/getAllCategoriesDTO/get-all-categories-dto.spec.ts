import { GetAllCategoriesDTO } from './get-all-categories-dto';

describe('GetAllCategoriesDTO', () => {
  it('should create an instance', () => {
    expect(new GetAllCategoriesDTO()).toBeTruthy();
  });
});
