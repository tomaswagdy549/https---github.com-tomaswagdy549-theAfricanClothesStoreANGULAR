import { GetAllSubCategoriesDTO } from './get-all-sub-categories-dto';

describe('GetAllSubCategoriesDTO', () => {
  it('should create an instance', () => {
    expect(new GetAllSubCategoriesDTO()).toBeTruthy();
  });
});
