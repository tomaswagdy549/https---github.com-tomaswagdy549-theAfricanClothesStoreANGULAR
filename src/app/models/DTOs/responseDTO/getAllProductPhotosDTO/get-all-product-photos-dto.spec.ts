import { GetAllProductPhotosDTO } from './get-all-product-photos-dto';

describe('GetAllProductPhotosDTO', () => {
  it('should create an instance', () => {
    expect(new GetAllProductPhotosDTO()).toBeTruthy();
  });
});
