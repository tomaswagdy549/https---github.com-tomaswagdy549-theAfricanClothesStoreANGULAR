import { UserRegisteredDTO } from './user-registered-dto';

describe('UserRegisteredDTO', () => {
  it('should create an instance', () => {
    expect(new UserRegisteredDTO()).toBeTruthy();
  });
});
