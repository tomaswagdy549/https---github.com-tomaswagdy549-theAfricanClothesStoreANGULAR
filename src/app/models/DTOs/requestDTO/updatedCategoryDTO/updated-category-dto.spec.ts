import { UpdatedCategoryDTO } from './updated-category-dto';

describe('UpdatedCategoryDTO', () => {
  it('should create an instance', () => {
    expect(new UpdatedCategoryDTO()).toBeTruthy();
  });
});
