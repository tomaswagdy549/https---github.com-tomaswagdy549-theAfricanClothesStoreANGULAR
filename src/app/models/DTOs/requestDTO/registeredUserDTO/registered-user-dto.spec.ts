import { RegisteredUserDTO } from './registered-user-dto';

describe('RegisteredUserDTO', () => {
  it('should create an instance', () => {
    expect(new RegisteredUserDTO()).toBeTruthy();
  });
});
