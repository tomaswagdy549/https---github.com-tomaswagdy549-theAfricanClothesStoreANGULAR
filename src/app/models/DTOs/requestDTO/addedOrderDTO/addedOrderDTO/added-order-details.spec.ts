import { AddedOrderDetails } from './added-order-details';

describe('AddedOrderDetails', () => {
  it('should create an instance', () => {
    expect(new AddedOrderDetails()).toBeTruthy();
  });
});
