import { UpdatedProductDTO } from './updated-product-dto';

describe('UpdatedProductDTO', () => {
  it('should create an instance', () => {
    expect(new UpdatedProductDTO()).toBeTruthy();
  });
});
