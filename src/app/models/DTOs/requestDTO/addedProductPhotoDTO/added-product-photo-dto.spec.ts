import { AddedProductPhotoDTO } from './added-product-photo-dto';

describe('AddedProductPhotoDTO', () => {
  it('should create an instance', () => {
    expect(new AddedProductPhotoDTO()).toBeTruthy();
  });
});
