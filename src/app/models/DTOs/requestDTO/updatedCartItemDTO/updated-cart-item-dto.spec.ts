import { UpdatedCartItemDTO } from './updated-cart-item-dto';

describe('UpdatedCartItemDTO', () => {
  it('should create an instance', () => {
    expect(new UpdatedCartItemDTO()).toBeTruthy();
  });
});
