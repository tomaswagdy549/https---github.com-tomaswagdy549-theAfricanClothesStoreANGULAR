import { ProductAvailableSizes } from './product-available-sizes';

describe('ProductAvailableSizes', () => {
  it('should create an instance', () => {
    expect(new ProductAvailableSizes()).toBeTruthy();
  });
});
